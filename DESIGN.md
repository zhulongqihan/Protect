# Design System

## Direction

这是一个在桌面浏览器中使用的本地维护工具。使用场景是晚上或工作间隙，用户只想快速判断电脑是否需要处理，再逐项检查清理建议。视觉应像一张安静、可信的工程值班台：信息有层次，状态有证据，动作有边界。

## Color

使用 restrained product palette。背景为纯白，正文为深色中性，面板使用低彩度中性层。主色锚定蜂蜜橙种子 oklch(0.773 0.157 56.6)，用于选中状态和主要操作；第二强调色使用深蓝，避免与主色混淆。成功、关注、紧急和未核验状态分别使用明确的语义颜色，并同时显示文字和图标。

实现时所有颜色使用 OKLCH，并验证正文至少 7:1、普通文字至少 4.5:1 的对比度。中等亮度的饱和色填充使用白色文字。

## Typography

使用 Segoe UI、system-ui、sans-serif 单一无衬线字体族。界面采用固定 rem 比例，正文保持易读行高；标题使用 text-wrap balance，长说明使用 text-wrap pretty。

## Layout

页面采用顶部状态栏、主结论区、磁盘状态行、清理审核列表、保护检查表和历史趋势区。使用列表、表格和展开面板表达信息，避免重复的同尺寸卡片网格。桌面宽度下保留证据列，窄屏下改为纵向条目。

## Interaction

所有按钮具备默认、悬停、焦点、按下、禁用和错误状态。清理页面只有导出审批文件的动作，不直接修改文件。加载状态使用骨架提示，错误状态说明恢复办法。动画只用于状态变化和展开，并尊重 prefers-reduced-motion。
