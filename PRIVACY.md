# 隐私说明

Protect Computer 的运行数据属于本机数据，不属于公开源码。

## 本机保存的内容

运行后，`runtime/` 可能包含：

- 磁盘盘符、容量和可用率；
- 系统版本、设备名、启动时间和检查时间；
- 防火墙、Windows Update、按需杀毒和物理磁盘的状态；
- 清理候选项的完整路径、大小、修改时间和审批结果；
- 最近 90 次本地报告的趋势摘要。

这些内容只为本机页面和审批校验服务。`.gitignore` 会忽略整个 `runtime/`，安全检查器也会拒绝把它当作公开文件。

## 公开内容

公开仓库只允许以下内容：

- 页面和 PowerShell 源码；
- 不含真实机器信息的文档；
- `sample/` 中使用占位符的合成示例；
- MIT 许可证、开发配置、Git hooks 和 GitHub Actions 安全检查。

公开检查器会阻断常见私钥、令牌、秘密赋值、当前用户路径、当前用户名、非允许路径和符号链接。它只报告命中类别、相对文件名和提交编号，不把秘密原文打印到终端。

## 发布前检查

```powershell
.\scripts\Check-PublicSafe.ps1 -History
git status --short
git diff --cached --name-only
```

如果检查失败，先删除或移出本机数据，再重新运行检查。不要把真实 `runtime/` 文件改名后加入提交；名称和内容都可能携带个人信息。
